import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.torquatoti',
  appName: 'VitorF',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
